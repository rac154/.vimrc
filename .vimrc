syntax enable "This turns on syntax highlighting
set tabstop=2 "This sets the number of spaces that each tab is
set softtabstop=2 "This is the number of spaces that are entered instead of tabs
set expandtab "This will turn any tabs you enter into spaces (Derek this was made to deal with people like you)
